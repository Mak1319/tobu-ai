"""HTTP / protocol surface for the agent.

* ``agui`` -- the AG-UI event-streaming helper (also used by the CLI).
* ``agui_server`` -- the FastAPI app exposing ``POST /api/agui/run`` to the
  ``tobu-ai-ui`` chat wizard.
"""
